# Secrets required by deploy_backend.sh / deploy_backend_prod.sh / deploy_backend_prod.ps1
#
# Copy this file to .env.deploy (gitignored), fill in real values, then:
#   bash:       source .env.deploy && ./deploy_backend.sh
#   PowerShell: convert this into a .ps1 that sets $env:VAR = "..." for each line, then . it

DB_PASSWORD=
JWT_SECRET=
GEMINI_API_KEY=
GOOGLE_DRIVE_CLIENT_SECRET=
